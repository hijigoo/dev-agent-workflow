FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY meeting_api ./meeting_api
RUN python -m pip install --no-cache-dir .

EXPOSE 8000
CMD ["uvicorn", "meeting_api.main:app", "--host", "0.0.0.0", "--port", "8000"]

